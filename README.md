# automation-sandbox

Verify the Jenkins Container
docker ps -a

If the jenkins container is stopped, start it:

docker start jenkins
Open Jenkins

http://localhost:8080

Stop Jenkins (Optional)
docker stop jenkins